version: 1
frontend:
  phases:
    install:
      runtime-versions:
        nodejs: 18       
    preBuild:
      commands:
        - node -v       
        - npm ci           
    build:
      commands:
        - npm run build    
    baseDirectory: build  
    files:
      - '**/*'
